# 11/11

웹팩 설정이 쉬운게 아니다. 

```bash
$ npm install -g
어디에서나 사용할 수 있는 라이브러리 만들겠다.
$ npm install -g @vue/cli

$ vue --version
버전 확인

$ vue create youtube-browser

npm run serve
npm run build
$ npm run lint
정해놓은 규칙에 어긋나는 에러들을 찾아서 알려주는 것.

$ npm install axios
```

```json
"eslintConfig": {
    "rules": {
        "no-console": "off"
    },
    
이거는 따로 해준거
```

